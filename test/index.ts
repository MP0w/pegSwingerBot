import { expect } from "chai";
import { ethers } from "hardhat";

describe("Swinger", function () {
  it("should ....", async function () {
    const Swinger = await ethers.getContractFactory("Swinger");
    // ...
  });
});
